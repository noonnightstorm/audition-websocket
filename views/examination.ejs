<!DOCTYPE html>
<html>
  <head>
    <title></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script type="text/javascript" src="/javascripts/jquery-1.7.min.js"></script>
    <script type="text/javascript" src="/socket.io/socket.io.js"></script>
    <script type="text/javascript" src="/javascripts/examination.js"></script>
  </head>
  <body>
    <h1 class="exam-title">Are you Ready?</h1>
    <% var count = 1  %>
  	<% questions.forEach(function(question){ %>
      <% if(mark == "replication"){ %>
    		<% if(question.type == "0"){ %>
    			<% include admin_exam_topic_single %>
    		<% } %>
    		<% if(question.type == "1"){ %>
    			<% include admin_exam_topic_double %>
    		<%} %>
    		<% if(question.type == "2"){ %>
    			<% include admin_exam_topic_question %>
    		<% } %>
      <% } %>
      <% if(mark == "answer"){ %>
        <% if(question.type == "0"){ %>
          <% include exam_topic_single %>
        <% } %>
        <% if(question.type == "1"){ %>
          <% include exam_topic_double %>
        <%} %>
        <% if(question.type == "2"){ %>
          <% include exam_topic_question %>
        <% } %>
      <% } %>
      <% count++ %>
  	<% }) %>

    <div class="exam-exit-box">
      <p>
        Please make sure you save all answers before you exit!
      </p>
      <a href="/">Exit</a>
    </div>
  </body>
</html>